/**
 * Placeholder app root. Phase 0 only proves the toolchain boots, lints, tests,
 * and builds. Real routing/shell arrive in later phases (Phase 2+).
 */
export default function App() {
  return (
    <main>
      <h1>Training Platform</h1>
      <p>Frontend scaffold is running.</p>
    </main>
  );
}
